import { createTheme } from '@material-ui/core/styles';

const theme=createTheme({
    palette:{
        primary:{
            main:"#d71921"
        },
        secondary:{
            main:"#ffff"
        }
    },
    background:{
        default:"#ffff"
    }
})

export default theme;